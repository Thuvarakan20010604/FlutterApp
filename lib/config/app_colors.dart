import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const brand = Color(0xFF2563EB);
  static const brandSoft = Color(0xFF60A5FA);
  static const success = Color(0xFF16A34A);
  static const danger = Color(0xFFDC2626);

  // Light
  static const bg = Color(0xFFF6F7FB);
  static const surface = Colors.white;
  static const text = Color(0xFF0B1220);
  static const subtext = Color(0xFF6B7280);
  static const border = Color(0xFFE7EAF2);

  // Dark
  static const bgDark = Color(0xFF0B1220);
  static const surfaceDark = Color(0xFF0F172A);
  static const textDark = Color(0xFFF8FAFC);
  static const subtextDark = Color(0xFFA1A1AA);
  static const borderDark = Color(0x3323344D);
}
